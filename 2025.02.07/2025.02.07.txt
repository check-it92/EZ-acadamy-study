2025.02.07

** 쿠키와 세션의 개념 다시 적기!!
 서버에서 전달되는 개념들

쿠키 : 기본적으로 날짜 정보 데이터가 필요합니다.
>> 서버 => 클라이언트 에게 쿠키를 전달한 이후, 해당 쿠키가 천년 만년 계속 유지x


세션 :  


const month = date.getMonth() +1 ; 달의 값은 컴퓨터가 인식하는 달의 인덱스 값은 0 = 1월로 인식하기
때문에 우리는 +1 을 해줘야 계산상 맞아진다.

const day = "0" + date.getDate(); // 이렇게 되면 '02' 이런식으로 되기 때문에 나중이 12월 처럼 2자리가 되었을때 방지할수 있다. 처음부터 2자리로 규제를 해놨기 때문에

expireDate.setDate(expireDate.getDate() + 1);  
// 하루를 기점으로 삼았기 때문에 +1 이 되는 것이다 바로 다음날 그리고 
자동적으로 현재시간에 +9시간이 붙기 때문에 다음날의 현재시간이 되는 것이다.

expireDate.toUTCString(); 이게 최신의 표준시간대 함수 expireDate.toGMTString();
.cookie.split(";") 내가 원하는 부분을 쪼개주는 함수 이고 
(";") 이안에 역할이 그 쪼개지는 구분을 해주는 역할 => 구분자라 한다.

코딩테스트 문제
JS코딩 테스트 문제 2
길이가 서로 다른 A, B, C 세 개의 막대 길이가 주어졌을 때, 이 세 막대로 삼각형을 만들 수 있다면 콘솔창에 "Yes"를 출력하고, 만들 수 없으면 "No"를 출력한다.
(*삼각형이 되려면 두 변의 길이의 합이 나머지 한 변의 길이 값보다 커야합니다.)

JS코딩 테스트 문제 3
연필 1다스는 12자루입니다. 학생 1인당 연필을 1자루씩 나누어 준다고 할 때, N명의 학생수를 입력한다면 필요한 연필의 다스 수를 계산하는 프로그램을 작성하세요.
(예를 들어 25명의 학생수가 있다면 3다스가 필요합니다. / 178명의 학생수가 있다면 15다스가 필요합니다.)