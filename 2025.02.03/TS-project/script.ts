// type notation = 타입주석
// 꼭 타입 주석을 입력하지 않더라도 기본적으로 ts는 타입추론을 할수 있다.
// 빨간줄로 오류가 나는 부분에 마우스를 올리면 해당 각주를 볼 수 있고 그에대한 추론을 할 수 있다.

let num: Number = 1;
let str: string = "Hello";
let bool = true;

bool = false;