const form = document.querySelectorAll("form");
const nums = ["num-a", "num-b", "num-c"];

form.addEventListener("submit", (e) => {
  e.prevertDefault();
  const inputText = document.querySelector("input[type='text']");
  form.forEach(() => {});
});
